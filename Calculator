#Task 1: Create the function subtraction and call it so that it can print out the result of the subtraction of two numbers.
#Task2: Create button2 and call the function subtraction so that it can be used as a button to perform the subtraction.

import tkinter as tk
def add():
    answer.config(text="I perform addition")

window=tk.Tk()
window.title("Calculator Application")
window.geometry("400x300")
text=tk.Label(window,text="Calculator")
text.pack()

button1=tk.Button(window,text="+",command=add)
button1.pack()

answer=tk.Label(window,text="Answer")
answer.pack()
window.mainloop()
