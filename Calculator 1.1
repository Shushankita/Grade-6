#Task 1: Add the button widget to the window and the 
#text on the button  should be a "+" sign.
#Task 2 : If a button is not visible on the screen 
#find out the reason behind it. hint of 
#code is available in the given code itself.

import tkinter as tk
window=tk.Tk()
window.title("Calculator Application")
window.geometry("400x300")
text=tk.Label(window,text="Calculator")
text.pack()

#uncomment the next line for the button. 
#button=tk.Button(window,text="+",command=add)
#button.pack()

answer=tk.Label(window,text="Answer")
answer.pack()
window.mainloop()
